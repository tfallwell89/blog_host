# Copy this file to apps/foodblog/.env before running the app.
#   cp .env.example apps/foodblog/.env

DATABASE_URL="postgresql://bloghost:bloghost_dev@localhost:5432/bloghost?schema=public"

# Base URL used when building absolute links (canonical URLs, public blog links).
APP_URL="http://localhost:3000"
